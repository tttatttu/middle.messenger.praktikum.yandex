<input
  type={{type}}
  id={{id}}
  name={{name}}
  minlength={{minlength}}
  maxlength={{maxlength}}
  required
  value={{#if value}}{{value}}{{else}}''{{/if}}
  placeholder={{placeholder}}
  class='input {{className}}'
/>
